import axios from "axios";
import { Request, response } from "express";
import { HiAnime } from "aniwatch";

export const hianime = async (req: Request, res: Response) => {

    try {
        const data = axios.get('');
    } catch (error) {
        
    }
};